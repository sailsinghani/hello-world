#!/groovy

node('master') {
  stage('cleanup') {
      echo "nothing to compile for hello.sh..."
    }
  stage('build') {
  echo "Hello world! I am node ${env.NODE_NAME}"
  }
  stage('Junit ') {
      echo "nothing to compile for hello.sh..."
    }
stage('Code Coverage') {
      echo "nothing to compile for hello.sh..."
    }
stage('Deploy') {
      echo "nothing to compile for hello.sh..."
    }

}
